const ActivityTitle = ({ activityTitle }) => {
  return (
    <span className="text-xl font-medium whitespace-nowrap leading-5 max-[402px]:text-lg">{activityTitle}</span>
  )
  
};

export default ActivityTitle;
